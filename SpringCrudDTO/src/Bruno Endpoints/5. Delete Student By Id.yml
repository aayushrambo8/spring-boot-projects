info:
  name: Delete Student By Id
  type: http
  seq: 5

http:
  method: DELETE
  url: http://localhost:8080/api/student/delete?id=
  params:
    - name: id
      value: ""
      type: query

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true

docs: Hard delete a student by ID. Returns 404 if not found.
