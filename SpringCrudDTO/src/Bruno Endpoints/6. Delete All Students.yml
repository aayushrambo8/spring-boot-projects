info:
  name: Delete All Students
  type: http
  seq: 6

http:
  method: DELETE
  url: http://localhost:8080/api/student/deleteAll

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true

docs: Hard delete all student records.
