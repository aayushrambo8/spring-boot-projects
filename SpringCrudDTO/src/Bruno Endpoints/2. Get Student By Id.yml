info:
  name: Get Student By Id
  type: http
  seq: 2

http:
  method: GET
  url: http://localhost:8080/api/student/get?id=5
  params:
    - name: id
      value: "5"
      type: query

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true

docs: Retrieve a student by ID. Returns 404 if not found.
