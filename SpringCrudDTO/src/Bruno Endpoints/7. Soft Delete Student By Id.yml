info:
  name: Soft Delete Student By Id
  type: http
  seq: 7

http:
  method: PATCH
  url: http://localhost:8080/api/student/soft-delete?id=4
  params:
    - name: id
      value: "4"
      type: query

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true

docs: Soft delete a student by ID. Returns 404 if not found.
