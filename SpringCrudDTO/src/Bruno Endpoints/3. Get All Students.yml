info:
  name: Get All Students
  type: http
  seq: 3

http:
  method: GET
  url: http://localhost:8080/api/student/getAll

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true

docs: Retrieve all students. Returns 404 if the list is null.
